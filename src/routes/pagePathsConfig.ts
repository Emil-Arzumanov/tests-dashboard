/**
 * Defines the paths for the application's pages.
 * @property testsLists The path for the Dashboard page, which displays the list of tests.
 * @property testResults The path for the Test Results page, which displays detailed results for a specific test.
 * @property testFinalize The path for the Test Finalize page, which allows finalizing a specific test.
 */
export const pagesPaths = {
	testsLists: "/dashboard",
	testResults: "/results",
	testFinalize: "/finalize",
};
